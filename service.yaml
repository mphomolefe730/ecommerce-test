apiVersion: v1
kind: Service
metadata:
  name: ecommerce-connect-service
spec:
  selector:
    app: ecommerce-connect
  ports: 
    - protocol: TCP
      port: 8080
      targetPort: 80
  type: LoadBalancer
