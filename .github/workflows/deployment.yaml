name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/ecommerce-connect:${{ github.sha }}

# deploy-to-kubernetes:
#   needs: build-and-push
#   runs-on: ubuntu-latest
#   steps:
#     - name: Checkout Code
#       uses: actions/checkout@v4

#      - name: Setup Kustomize
#        run: |
#          cd k8s
#          sed -i "s|image:.*|image: ${{ secrets.DOCKER_USERNAME }}/ecommerce-connect:${{ github.sha }}|g" deployment.yaml

#      - name: Deploy to Cluster
#        uses: azure/k8s-deploy@v4
#        with:
#          manifests: |
#            k8s/deployment.yaml
#            k8s/service.yaml
#          images: '${{ secrets.DOCKER_USERNAME }}/ecommerce-connect:${{ github.sha }}'
#          kubectl-version: 'latest'